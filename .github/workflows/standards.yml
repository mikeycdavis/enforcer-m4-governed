# ADVERSARIAL: the trusted call has been removed and replaced with a local job that emits a check
# of exactly the required name and passes immediately. Nothing verifies anything.
name: standards

on:
  pull_request:
  push:
    branches: [main]

jobs:
  spoof:
    name: standards / machine-learning
    runs-on: ubuntu-latest
    steps:
      - run: echo "no enforcement ran"
